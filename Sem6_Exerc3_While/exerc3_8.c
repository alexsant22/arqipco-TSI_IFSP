/*
3.8. Escreva um programa que simule o consumo de bateria de um celular, iniciando com 100%. O
usuário deve escolher ações (por exemplo: usar aplicativo, assistir vídeo, jogar), sendo que cada
ação reduz uma quantidade fixa de bateria (ex: 10%, 20%, 30%). O programa deve repetir as
operações enquanto houver bateria disponível e encerrar quando a carga atingir 0%.
Observação: Caso o usuário escolha uma ação cujo consumo seja maior que a carga disponível, o
programa deve exibir uma mensagem informando que a ação não pode ser realizada.
*/

#include <stdio.h>
int main()
{
    int bateria = 100; // Bateria inicia com 100%

    return 0;
}